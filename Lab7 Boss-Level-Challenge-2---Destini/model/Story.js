class Story{
    constructor(storyTitle, firstChoice, secondChoice){
        this.storyTitle = storyTitle;
        this.firstChoice = firstChoice;
        this.secondChoice = secondChoice;
    }
}

export default Story;